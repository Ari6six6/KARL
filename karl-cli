#!/usr/bin/env bash
# karl — run the harness with no install:  ./karl-cli
# (or install it and get a bare `karl`:  pip install -e .)
here="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec env PYTHONPATH="$here${PYTHONPATH:+:$PYTHONPATH}" python3 -m karl "$@"
